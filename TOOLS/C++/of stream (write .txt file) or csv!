#include <fstream>

// Write weights to a txt file
std::ofstream f_txt("Weights_TotalLogLikelihood.txt");

for (size_t i = 0; i < wgts_totalLogLikelihood.size(); ++i) {
    f_txt << "Flux " << i << " " << wgts_totalLogLikelihood[i] << '\n';
}

f_txt.close();
