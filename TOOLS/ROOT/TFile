**********
Python
**********



**********
C++
**********

To create a new one:
TFile* f_out = new TFile("../output/InclusiveXsec_CV.root", "RECREATE");

#include <TFile.h>
TFile* file = TFile::Open("data.root", "READ");

// Read an object from the file
PlotUtils::MnvH1D* EePred = dynamic_cast<PlotUtils::MnvH1D*>(f_EePred->Get(histo_name));

file->Close();
delete file;
