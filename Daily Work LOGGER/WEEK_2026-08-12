Wednesday, August 12, 2026

--> Email from Kevin, for now keep going with the cross-sections that we have, will need to fix according to what Sasha 
sends

GOAL for the Day
--> Make progress on getting Luis' code to work
--> Make the total electron energy distributions for the 4 modes, 1 for each flavour and then add them up and send to 
Deborah
--> Review what I've learned, where the next steps are, document


Question: What do the total electron energy distributions look like for the 4 modes? Investigate how to cap the bin edge at 
20 GeV, but put everything above 20 GeV into that bin. I need the flux histograms for the 4 modes with the 4 flavours. 

Tried: Got gen2thin files from Quinn and created a function that saves those fluxes to a file and then a plotting script
for plotting them

Learned: Can create a function of an object and then pass it in inputs after the object gets initialized to get the 
function to do something. Like can write all of the fluxes from external input to create a flux file just using the object
that was initialized by using a function created for that object. Can check the GetMaximum() for the max y value to 
adjust the y scale when plotting multiple histos on same plot.

Produced: Produced the plots for LE fluxes for all 4 flavours in LE RHC, LE FHC

Next Steps: -> Call the function to write ME flux files
            -> Plot the ME fluxes with the plotting script
            -> Change the defn of EeDistributionCreator and the LoadFluxHistos() method to correctly  be loading in 
               the flux histograms and make the EeDistributions with the loaded histograms w/o fixing the OF/UF business
               to ensure the flux histograms are being read in correctly
            -> Determine the mapping for the OF and UF for the various histograms.
            -> Write out a plan to investigate what the error looks like on those bins. 
            -> Add the overflow content to the last bin, added the overflow error to the last bin error, cleared the overflow


For the radiative corrections:
--> Added a function that allows for the EeDistributionCreator object to be called and write the radiative correction
for all 4 flavours for a particular neutrino energy to a file
--> The function is called: // write the rad correction for fixed neutrino energy to file
      void WriteRadCorrToFileForFixedEnu(TString path, TString output_name, double Enu);

--> It gets called be doing (after the radiative correction objects get initialized obviously so that the radiative correction
function is loaded for each mode)
creator.WriteRadCorrToFileForFixedEnu(output_path, "RadCorr_Enu10GeV",10.0);

--> And then it's plotted by calling the plotting script several times using: . plotRad.sh

**************************
Thursday, August 13, 2026
**************************

--> Need some blurb on the flux files: gen2thin --> FOR NOW: Just note down that using the gen2thin flux
    ME is the gen2thin fluxes from FRW and not the rearranged_universes
--> Send the code to git: ENTRY made in git specifics folder
--> Get the total electron energy distributions for each mode for all the universes
--> Write out how the machinery is working: Update the machinery to how it should be working first
    -about the POT

--> What I got done:
--> Sorted out business with OF and UF and checked that able to reproduce the same solutions as before with the matrices
--> Added up the 4 electron energy distributions to get the total one and plotted them
--> Working on getting the Ee distributions for all of the universes, solved the bug with the errorband name, currently
on the step of looping over and filling in each universe for the Ee distribution, that is currently crashing, also need to
see what number of universes should be used for each flavour in a particular mode: diff # of flux universes for diff fluxes?
--> TODO: Can write up summary of the overflow and underflow notes from today
--> need to go over how to better structure the code

