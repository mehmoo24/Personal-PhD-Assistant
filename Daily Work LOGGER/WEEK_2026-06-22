Technical Notes Review:

--> If we want to draw the error as a band then we still use 'EP' but this time we can use a fill colour it should plot it.
'EP' already knows to create a symmetric error band and so it's a waste of time to use the uncertainty and start creating
an error band yourself
see. /exp/minerva/app/users/mmehmood/MAT_AL9/GitRepo-Analysis/ana/FLUX/Flux_Plotting/flux_CV_RMS.py

--> For covariance matrix, can use nbins, xmin, xmax to define the binning for the 2d covariance matrix
see. /exp/minerva/app/users/mmehmood/MAT_AL9/GitRepo-Analysis/ana/FLUX/MakeCovMatrix

--> Turns out the covariance matrix is taking very long to fill in
--> The question was how the covariance changes when we move around the correlations --> the impact that has on the result
after applying Luis' flux constraint

--> BAD IDEA to use python to fill in such a huge covariance matrix, better bet to write c++ file and then submit it to
    the grid
--> For Quinn's FluxCalculator will need to run the code again, this time offset the flux correlations for the focusing
    uncertainties and submit that to the grid as well

--> Only keep the necessary files from Quinn's directory b/c otherwise it is 1.4 GB

PICK UP NOTES:
--> CHeck the grid submission for just running over Quinn's code
--> Submit to the grid the code for writing a covariance matrix (need to write that code in C++ first)
--> Make the change to FluxCalculator for breaking the correlation and submit that code to the grid as well
DELIVERABLES: 
* Make the covariance plot (by submitting to the grid) for the case of breaking the correlation to double check
that the correlation was in fact broken for the focusing uncertainties between neutirno mode and antineutrino mode
* Apply Luis' constraint to 3 cases: shuffled universes for neutrino mode, shuffled universes for antineutrino mode, 
shuffled universes for both to see how shuffling the universes has impacted the application of the constraintcd


