Structure:
8-9am: Review Physics + Questions about concepts
9am - 9:30am: coding practice / fix structure of code
9:30am - 9:40am: walk to library
9:40am - 12:40pm: work block
12:40pm - 1:10pm: lunch
1:10pm - 1:45pm: review + writing block

Monday, August 17, 2026

--> Get the Ee distributions using the method that I already have --> it causes a memory crash but I get the Ee distribution
for the CV and the universes
GOAL:
--> Get the total Ee distribution
--> Get the plot with the uncertainty plotted out on the plot, maybe don't need to plot this out
--> Sent Deborah the Ee distributions, should bin width normalize going forward

Should have as ready input:
1. Files with the Ee Distributions for all 4 modes
2. The 4 flux files

PICK UP POINT: Get the formulas for the log likelihood and implement them, cool thing is then create a histo
and fill in with the values that put in a vector when getting the loglikelihood of each universe, get three
histograms like that
TODO:
--> loglikelihood distributions, need to get Luis' expected distribution and read it in
--> covariance matrices
--> other distribution Kevin said, send those off
--> have the writeup for these


IN Notebook by the end of the week:
--> Section on likelihood and log-likelihood
* the likelihood function tells us that the data is a given and the model is what varies, we determine the 
likelihood that the data is well described by the chosen parameters of the model. Even for a good likelihood, it
doesn't mean that the model was a good choice, we can have the wrong model and still find the best values of the
wrong model that best describe the data observed. so it's out of the parameters given, which ones best describe 
the data. Ex. best shoe out of a small set of shoes, the best shoe may still be too small
* We are multiplying probabilities together because the multiplication is for independent observations
* The log is a good approach because it changes the multiplication into addition which is useful when there's
a lot of observations and need to multiply a lot of numbers together, the product can get really small and then
it can get computationally hard
* Need to pick a model when computing the likelihood
--> Section on 4 distributions and when to use them: Bernoulli, Binominal, Poisson, Gaussian
* Bernoulli is for a yes or no type result
* Binomial is a bunch of bernoulli results put together, each one individually was a yes or no
Q: What is poisson?
Q: For the bin log-likelihood, how come we add for all the bins instead of multiplying? and how does subtracting
the two give the shape log-likelihood?
Q: The progression of the formulas to get the one that is finally for comparing electron energy distribution w/
the other one
--> Covariance matrices
Q: Don't quite understand the formula
Q: Interpretation of the results


TODO: The code for breaking the flux correlations, and run it even when running with the FRW flux?
Q: Why to use lgamma(n+1) instead of the factorial?

Wednesday, August 19, 2026
-> Run Luis' code
-> See the impact of running Luis' code on shuffled universes: Get the constraint for the shuffled univs
-> Send off the output

TODO: Add in the IMD cross-section

Q: What are the existing weights of the universes?
Q: how does using the likelihood compare to using a chi2 weight?
Q: How to derive Bayes' theorem? The denominator gives us the total probability, but if none of the universes are likely
to give us the data?
